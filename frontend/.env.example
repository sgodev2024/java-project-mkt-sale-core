# Base URL của Core Platform backend mà Control Plane UI gọi tới.
# Local development (docker compose up ở repo root):
NEXT_PUBLIC_CORE_API_URL=http://localhost:8080

# Production hiện đang dùng API domain riêng, ví dụ:
# NEXT_PUBLIC_CORE_API_URL=https://api.corejava.sgodata.com
