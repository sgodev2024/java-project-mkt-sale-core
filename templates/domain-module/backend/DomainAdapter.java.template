package __BASE_PACKAGE__;

import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.stereotype.Component;
import vn.coreplatform.kernel.DomainResourceAdapter;
import vn.coreplatform.kernel.ResourceDescriptor;

@Component
public class DomainAdapter implements DomainResourceAdapter {
  @Override public ResourceDescriptor descriptor() {
    return new ResourceDescriptor(
        "__RESOURCE_TYPE__", "__RESOURCE_NAME__", "__MODULE_KEY__", "DOMAIN", "1.0.0",
        List.of("READ", "CREATE", "UPDATE", "ARCHIVE"), "ALWAYS", "CONFIDENTIAL");
  }

  @Override public Optional<Snapshot> find(UUID tenantId, String resourceId) {
    throw new UnsupportedOperationException("Implement domain-owned lookup");
  }

  @Override public List<HistoryEntry> history(UUID tenantId, String resourceId, int limit) {
    throw new UnsupportedOperationException("Implement domain-owned history");
  }
}

